sudo docker compose build --no-cache && sudo docker compose up -d


1. 백엔드 (Spring Boot) - 도커 재시작 및 깃 커밋
백엔드 로직(GeminiOrchestrationService 등)이 변경되었으므로 백엔드 서버 재시작이 먼저 필요합니다.

작업 경로: /home/ubuntu/root/base/travel
해야 할 일:
빌드 확인 (선택): 기존 Dockerfile 설정에 따라 다르지만, 컨테이너를 빌드하기 전 Maven(mvn clean package) 등을 통해 새로운 .jar 파일로 빌드해야 할 수도 있습니다. (Dockerfile이 소스코드를 내부에서 직접 빌드한다면 생략 가능)
도커 재시작: 터미널에서 해당 경로(/home/ubuntu/root/base/travel)로 이동한 뒤, 텍스트 문서에 적힌 명령어를 실행하여 백엔드 API 서버를 재배포합니다.
bash
cd /home/ubuntu/root/base/travel
sudo docker compose build --no-cache && sudo docker compose up -d
깃 커밋 & 푸시: 서버 정상 구동 확인 후 해당 경로에서 백엔드 변경 사항을 커밋합니다.
bash
git add .
git commit -m "feat: 여행 계획 1일 추천 장소 개수 동적 설정 API 파라미터 추가"
git push
2. 프론트엔드 (Android) - 앱 테스트 및 깃 커밋
프론트엔드는 안드로이드 코드이므로 도커와는 무관하며, 안드로이드 스튜디오 등에서 앱을 빌드해야 합니다.

작업 경로: /home/ubuntu/root/base/Harudiary_trip2
해야 할 일:
앱 빌드 및 테스트: 안드로이드 스튜디오에서 앱을 다시 빌드(Run)하여 핸드폰이나 에뮬레이터에 설치합니다. 새로 추가된 UI(슬라이더 개수 조절), 다이어리 탭의 AI 일정 버튼(✨), 로딩 화면 등이 에러 없이 작동하는지 확인합니다.
깃 커밋 & 푸시: 테스트가 이상 없이 완료되면 프론트엔드 변경 사항을 커밋합니다.
bash
cd /home/ubuntu/root/base/Harudiary_trip2
git add .
git commit -m "feat: 다이어리 연동 AI 여행계획 생성, 삭제 복구(Undo), 추천 장소 개수 UI 조절 및 로딩 추가"
git push
💡 요약하자면: 빌드 명령어.md에 적힌 도커 재시작 명령어는 백엔드 폴더(.../base/travel) 안에서 실행하셔야 하며, 프론트엔드와 백엔드가 별개의 프로젝트(Git)이므로 커밋과 푸시 역시 각각의 폴더에서 따로따로 한 번씩(총 2번) 진행하셔야 합니다!